
# Caddyfile

# For local development, we use :80 (localhost).
# For production, replace ':80' with your actual domain name (e.g., example.com).
# Caddy will automatically obtain an SSL certificate for your domain.

:80 {
    # Reverse proxy /api requests to the backend service
    handle_path /api/* {
        reverse_proxy backend:8000
    }

    # Reverse proxy all other requests to the frontend service
    handle {
        reverse_proxy frontend:3000
    }
}
